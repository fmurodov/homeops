---
# yaml-language-server: $schema=https://json.schemastore.org/kubernetes
apiVersion: v1
kind: Service
metadata:
  name: homebox-http
  labels:
    app.kubernetes.io/name: homebox
spec:
  ports:
    - port: 7745
      targetPort: 7745
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: homebox
