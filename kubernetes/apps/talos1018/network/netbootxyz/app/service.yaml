---
apiVersion: v1
kind: Service
metadata:
  name: netbootxyz
  labels:
    app.kubernetes.io/name: netbootxyz
  annotations:
spec:
  ipFamilyPolicy: PreferDualStack
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
    - port: 80
      targetPort: http-assets
      protocol: TCP
      name: http-assets
  selector:
    app.kubernetes.io/name: netbootxyz
---
apiVersion: v1
kind: Service
metadata:
  name: netbootxyz-udp
  labels:
    app.kubernetes.io/name: netbootxyz
    service.kubernetes.io/topology: internal
  annotations:
    io.cilium/lb-ipam-ips: "fd00:1018:0:5:3000::43,10.18.6.43"
spec:
  externalTrafficPolicy: Local
  sessionAffinity: None
  type: LoadBalancer
  ipFamilyPolicy: PreferDualStack
  ports:
    - port: 69
      targetPort: tftp
      protocol: UDP
      name: tftp
  selector:
    app.kubernetes.io/name: netbootxyz
