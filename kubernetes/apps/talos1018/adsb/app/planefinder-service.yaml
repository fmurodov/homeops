---
# yaml-language-server: $schema=https://json.schemastore.org/kubernetes
apiVersion: v1
kind: Service
metadata:
  name: planefinder
  namespace: adsb
spec:
  selector:
    app.kubernetes.io/name: planefinder
  ports:
    - port: 30053
      targetPort: 30053
      protocol: TCP
      name: http
