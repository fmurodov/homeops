---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./namespace.yaml

  # Exporters (no deps — own their HelmRepositories)
  - ./kube-state-metrics/ks.yaml
  - ./node-exporter/ks.yaml

  # Storage backends (depend on exporters for scraping)
  - ./victoria-metrics/ks.yaml
  - ./victoria-logs/ks.yaml

  # Alerting
  - ./ntfy/ks.yaml
  - ./alertmanager/ks.yaml
  - ./vmalert/ks.yaml
  - ./flux-alerts/ks.yaml

  # Log collection
  - ./fluent-bit/ks.yaml

  # External probes
  - ./nrtm-check/ks.yaml

  # Visualization
  - ./grafana/ks.yaml
