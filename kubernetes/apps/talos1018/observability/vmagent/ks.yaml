---
# yaml-language-server: $schema=https://k8s-schemas.bjw-s.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
# vmagent - Metrics collection (replaces Prometheus scraper)
# Uses the 'vm' HelmRepository created by victoria-metrics
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: vmagent
  namespace: flux-system
spec:
  dependsOn:
    - name: victoria-metrics
    - name: kube-state-metrics
    - name: node-exporter
  interval: 1h
  retryInterval: 1m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./kubernetes/apps/talos1018/observability/vmagent/app
  prune: true
  wait: true
  postBuild:
    substituteFrom:
      - kind: Secret
        name: cluster-secrets
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: vmagent
      namespace: observability
