# S3 Credentials for Longhorn Backup Restoration
# Copy this file to ../.env.local (repo root) and fill in your credentials
# Get these values from: kubectl get secret longhorn-r2-backups -n longhorn-system -o yaml

# S3 Access Credentials
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_access_key_here

# S3 Endpoint (Cloudflare R2 format)
S3_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com

# Longhorn Backup Configuration
LONGHORN_S3_BUCKET=longhorn-backup
LONGHORN_S3_PREFIX=1018

# Note: .env.local should be in repo root (one level up from dr/)
# Keep it secure with: chmod 600 ../.env.local
